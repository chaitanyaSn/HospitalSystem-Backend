services:
  db:
    image:
      postgres:latest
    ports:
      - "5433:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: password
  adminer:
    image: adminer:latest
    ports:
      - "8888:8080"
    restart: always

